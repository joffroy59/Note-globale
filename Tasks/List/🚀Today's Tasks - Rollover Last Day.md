---
tags:
  - tasks
  - template
---
```tasks
filter by function return task.heading?.includes('🥷') || false

sort by function reverse task.file.filename 
group by path reverse 
```

```tasks
filter by function return task.heading?.includes('Rollover Last Day') || false

sort by function reverse task.file.filename 
group by path reverse 
```
 